# The plugin ships no runtime dependencies; peers are supplied by the harness
# profile. Failing loudly here is what keeps a stray dependency from being
# silently resolved from a transitive copy.
auto-install-peers=true
strict-peer-dependencies=true
